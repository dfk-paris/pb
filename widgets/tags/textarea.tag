<pb-textarea>

  <label>
    {opts.label}
    <textarea
      name={opts.name}
      placeholder={opts.label}
      class="u-full-width"
      value={opts.value}
    ></textarea>
  </label>

</pb-textarea>