<pb-dimension-values if={opts.width && opts.height && opts.depth} >
  
  <div class="dimension-values">
    <em>{opts.label}</em>:
    H {opts.height} cm x B {opts.width} cm x T {opts.depth} cm
  </div>

</pb-dimension-values>